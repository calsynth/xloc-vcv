diff --git a/software/src/Audio/AudioEffectModalResonator.h b/software/src/Audio/AudioEffectModalResonator.h
index ea92bb44..aa3fecd3 100644
--- a/software/src/Audio/AudioEffectModalResonator.h
+++ b/software/src/Audio/AudioEffectModalResonator.h
@@ -184,9 +184,11 @@ public:
         }
 
         // Enable FPU flush-to-zero + default-NaN
+#if defined (__ARM_ARCH_7EM__)
         uint32_t fpscr_save;
         __asm__ volatile("vmrs %0, fpscr" : "=r"(fpscr_save));
         __asm__ volatile("vmsr fpscr, %0" :: "r"(fpscr_save | 0x03000000u));
+#endif
 
         const int16_t* src = in ? in->data : nullptr;
         int16_t*       dst = out->data;
@@ -276,7 +278,9 @@ public:
             bp_[k] = bp[k];
         }
 
+#if defined (__ARM_ARCH_7EM__)
         __asm__ volatile("vmsr fpscr, %0" :: "r"(fpscr_save));
+#endif
 
         transmit(out, 0);
         release(out);
diff --git a/software/src/HSIOFrame.h b/software/src/HSIOFrame.h
index 8061f8c4..411dd951 100644
--- a/software/src/HSIOFrame.h
+++ b/software/src/HSIOFrame.h
@@ -833,7 +833,9 @@ struct IOFrame {
 
     // --- Soft IO ---
     int In(int ch) {
-      if (ch < ADC_CHANNEL_COUNT)
+      // current_ioframe is null until the first ISR Process(); reading
+      // through null is benign on IMXRT but fatal on host builds.
+      if (current_ioframe && ch < ADC_CHANNEL_COUNT)
         return current_ioframe->cv.pitch_values[ch];
       return 0;
     }
diff --git a/software/src/HemisphereApplet.cpp b/software/src/HemisphereApplet.cpp
index 2fd2735f..9b164ecc 100644
--- a/software/src/HemisphereApplet.cpp
+++ b/software/src/HemisphereApplet.cpp
@@ -31,20 +31,23 @@ void HemisphereApplet::BaseStart(const HEM_SIDE hemisphere_) {
 }
 void HemisphereApplet::BaseView(bool full_screen, bool parked) const {
     //if (HS::select_mode == hemisphere)
+    // (const_cast: View/DrawFullScreen are non-const virtuals overridden by
+    // every applet; changing their signatures is not practical)
+    HemisphereApplet* self = const_cast<HemisphereApplet*>(this);
     gfxHeader(applet_name(), (HS::ALWAYS_SHOW_ICONS || full_screen) ? applet_icon() : nullptr);
     // If active, draw the full screen view instead of the application screen
     if (full_screen) {
       if (parked)
-        this->DrawFullScreen();
+        self->DrawFullScreen();
       else
         DrawConfigHelp();
     }
-    else this->View();
+    else self->View();
 }
 
 void HemisphereApplet::DrawConfigHelp() const {
     for (int i=0; i<HELP_LABEL_COUNT; ++i) help[i] = "";
-    SetHelp();
+    const_cast<HemisphereApplet*>(this)->SetHelp();
     const bool clockrun = HS::clock_m.IsRunning();
 
     for (int ch = 0; ch < 2; ++ch) {
diff --git a/software/src/HemisphereApplet.h b/software/src/HemisphereApplet.h
index 7ed1c021..ae785f08 100644
--- a/software/src/HemisphereApplet.h
+++ b/software/src/HemisphereApplet.h
@@ -168,8 +168,8 @@ public:
     inline bool EditMode() const {
       return (enc_edit[hemisphere].isEditing);
     }
-    void SetLabel(const char *str) { enc_edit[hemisphere].label = str; }
-    void SetAux(bool aux) { enc_edit[hemisphere].aux_action = aux; }
+    void SetLabel(const char *str) const { enc_edit[hemisphere].label = str; }
+    void SetAux(bool aux) const { enc_edit[hemisphere].aux_action = aux; }
 
     template<typename T>
     void MoveCursor(T &cursor, int direction, int max) {
diff --git a/software/src/OC_autotuner.h b/software/src/OC_autotuner.h
index e7f77227..8ea7ea21 100644
--- a/software/src/OC_autotuner.h
+++ b/software/src/OC_autotuner.h
@@ -112,7 +112,7 @@ enum AUTO_CALIBRATION_STEP {
   AUTO_CALIBRATION_STEP_LAST
 };
 
-extern const char* const AT_steps[];
+extern const char* const AT_steps[11];
 extern const char *const reset_action_strings[];
 extern const char *const status_action_strings[];
 
diff --git a/software/src/OC_calibration.h b/software/src/OC_calibration.h
index d1b7d30c..5e363945 100644
--- a/software/src/OC_calibration.h
+++ b/software/src/OC_calibration.h
@@ -17,9 +17,9 @@ namespace OC {
 
 static constexpr uint16_t _ADC_OFFSET_NLM = 4095; // 0V == maximum 12-bit ADC value
 #ifdef NORTHERNLIGHT
-static constexpr uint16_t _ADC_OFFSET = (uint16_t)((float)pow(2,OC::ADC::kAdcResolution)*1.0f);   // ADC offset @3.3V
+static constexpr uint16_t _ADC_OFFSET = (uint16_t)(1u << OC::ADC::kAdcResolution);   // ADC offset @3.3V
 #else
-static constexpr uint16_t _ADC_OFFSET = (uint16_t)((float)pow(2,OC::ADC::kAdcResolution)*0.6666667f); // ADC offset @2.2V
+static constexpr uint16_t _ADC_OFFSET = (uint16_t)((1u << OC::ADC::kAdcResolution) * 2 / 3); // ADC offset @2.2V
 #endif
 
 static constexpr unsigned kCalibrationAdcSmoothing = 4;
diff --git a/software/src/OC_menus.cpp b/software/src/OC_menus.cpp
index d242eb00..9defb687 100644
--- a/software/src/OC_menus.cpp
+++ b/software/src/OC_menus.cpp
@@ -31,7 +31,12 @@ constexpr std::array<coords, sizeof...(Is)> generate_circle_pos_lut(util::index_
   return { generate_circle_coords<Is>()... };
 }
 
+#if defined(__clang__)
+// clang does not treat cosf/sinf as constexpr builtins; initialize at static init time
+static const std::array<coords, 12> circle_pos_lut = generate_circle_pos_lut(util::make_index_sequence<12>::type());
+#else
 static constexpr std::array<coords, 12> circle_pos_lut = generate_circle_pos_lut(util::make_index_sequence<12>::type());
+#endif
 
 namespace menu {
 void Init()
diff --git a/software/src/OC_strings.h b/software/src/OC_strings.h
index e8af0380..ee819649 100644
--- a/software/src/OC_strings.h
+++ b/software/src/OC_strings.h
@@ -32,8 +32,12 @@ namespace OC {
     extern const char * const note_names[];
     extern const char * const note_names_unpadded[];
     extern const char * const VOR_offsets[];    
-    extern const char * const trigger_input_names_none[];
-    extern const char * const cv_input_names_none[];
+#ifdef ARDUINO_TEENSY41
+    extern const char * const trigger_input_names_none[21];
+#else
+    extern const char * const trigger_input_names_none[13];
+#endif
+    extern const char * const cv_input_names_none[97];
     extern const char * const no_yes[];
     extern const char * const off_on[];
     extern const char * const scaling_string[];
diff --git a/software/src/apps/Calibr8or.h b/software/src/apps/Calibr8or.h
index c749fa1d..b51c1db8 100644
--- a/software/src/apps/Calibr8or.h
+++ b/software/src/apps/Calibr8or.h
@@ -752,9 +752,10 @@ public:
         gfxFrame(20, y-3, 64, 18);
         gfxIcon(23, y+2, positive? PLUS_ICON : MINUS_ICON);
 
-        segment.PrintWhole(33, y, abs(octave), 10);
+        auto& seg = const_cast<SegmentDisplay&>(segment);
+        seg.PrintWhole(33, y, abs(octave), 10);
         gfxPrint(53, y+5, ".");
-        segment.PrintWhole(61, y, abs(degrees), 10);
+        seg.PrintWhole(61, y, abs(degrees), 10);
 
         // Scale
         gfxIcon(89, y, SCALE_ICON);
diff --git a/software/src/apps/CaptainMIDI.h b/software/src/apps/CaptainMIDI.h
index a1074c9c..6f8a5c02 100644
--- a/software/src/apps/CaptainMIDI.h
+++ b/software/src/apps/CaptainMIDI.h
@@ -243,9 +243,10 @@ public:
 
     void View() const;
     void MainView() const {
-        if (copy_mode) DrawCopyScreen();
-        else if (display == 0) DrawSetupScreens();
-        else DrawLogScreen();
+        auto* self = const_cast<AppCaptainMIDI*>(this);
+        if (copy_mode) self->DrawCopyScreen();
+        else if (display == 0) self->DrawSetupScreens();
+        else self->DrawLogScreen();
     }
 
     void EncoderEdit(int dir) {
@@ -585,7 +586,7 @@ private:
     const char* const GetLabel(int pos) const {
       if (pos < DAC_CHANNEL_COUNT)
         return frame.MIDIState.mapping[pos].get_label();
-      return midi_out_functions[get_out_assign(pos - DAC_CHANNEL_COUNT)];
+      return midi_out_functions[const_cast<AppCaptainMIDI*>(this)->get_out_assign(pos - DAC_CHANNEL_COUNT)];
     }
 
     void DrawLogScreen() {
diff --git a/software/src/apps/Enigma.h b/software/src/apps/Enigma.h
index a5814a99..02604635 100644
--- a/software/src/apps/Enigma.h
+++ b/software/src/apps/Enigma.h
@@ -132,8 +132,9 @@ public:
 
     void View() const;
     void MainView() const {
-        DrawHeader();
-        DrawInterface();
+        auto* self = const_cast<AppEnigma*>(this);
+        self->DrawHeader();
+        self->DrawInterface();
     }
 
     // Public access to save method
diff --git a/software/src/apps/ScaleEditor.h b/software/src/apps/ScaleEditor.h
index 74099c3e..b79cf517 100644
--- a/software/src/apps/ScaleEditor.h
+++ b/software/src/apps/ScaleEditor.h
@@ -189,8 +189,9 @@ private:
                     uint32_t cents = (note_value * 100) >> 7;
                     uint32_t frac_cents = ((note_value * 100000) >> 7) - cents * 1000;
 
-                    tinynumbers.PrintWhole(x + 2, y + 2, cents);
-                    tinynumbers.PrintDecimal(frac_cents, 1, 1000);
+                    auto& tiny = const_cast<SegmentDisplay&>(tinynumbers);
+                    tiny.PrintWhole(x + 2, y + 2, cents);
+                    tiny.PrintDecimal(frac_cents, 1, 1000);
 
                     // If this is the current note, highlight it
                     if (ix == current_note && !length_set_mode) gfxInvert(x, y, 32, 9);
@@ -202,15 +203,16 @@ private:
 
         // Now the editing area:
         if (length_set_mode) {
-            segment.PrintWhole(22, 52, OC::user_scales[current_scale].num_notes);
+            const_cast<SegmentDisplay&>(segment).PrintWhole(22, 52, OC::user_scales[current_scale].num_notes);
         } else {
             // Show the value
             uint32_t note_value = OC::user_scales[current_scale].notes[current_note];
             uint32_t cents = (note_value * 100) >> 7;
             uint32_t frac_cents = ((note_value * 100000) >> 7) - cents * 1000;
 
-            segment.PrintWhole(12, 52, cents);
-            segment.PrintDecimal(frac_cents, 3, 1000);
+            auto& seg = const_cast<SegmentDisplay&>(segment);
+            seg.PrintWhole(12, 52, cents);
+            seg.PrintDecimal(frac_cents, 3, 1000);
         }
     }
 
diff --git a/software/src/apps/TheDarkestTimeline.h b/software/src/apps/TheDarkestTimeline.h
index 3681a5df..30cd3b47 100644
--- a/software/src/apps/TheDarkestTimeline.h
+++ b/software/src/apps/TheDarkestTimeline.h
@@ -243,7 +243,7 @@ public:
     void View() const;
     void MainView() const {
         gfxHeader("Darkest Timeline");
-        DrawInterface();
+        const_cast<TheDarkestTimeline*>(this)->DrawInterface();
     }
 
     /* When the app is suspended, it sends out a system exclusive dump, generated here */
diff --git a/software/src/audio_applets/WAVRecorderApplet.h b/software/src/audio_applets/WAVRecorderApplet.h
index e773c923..9c7f7736 100644
--- a/software/src/audio_applets/WAVRecorderApplet.h
+++ b/software/src/audio_applets/WAVRecorderApplet.h
@@ -476,7 +476,7 @@ private:
 
     bool QueuesAvailable() const {
         for (int ch = 0; ch < Channels; ch++)
-            if (!record_queue[ch].available()) return false;
+            if (!const_cast<AudioRecordQueue&>(record_queue[ch]).available()) return false;
         return true;
     }
 
diff --git a/software/src/src/Audio/filter_variable2.cpp b/software/src/src/Audio/filter_variable2.cpp
index 310f4949..2eb405d3 100644
--- a/software/src/src/Audio/filter_variable2.cpp
+++ b/software/src/src/Audio/filter_variable2.cpp
@@ -272,7 +272,7 @@ void AudioFilterStateVariable2::update(void)
 	return;
 }
 
-#elif defined(KINETISL)
+#else // portable fallback (XLOC2 emulator host build)
 
 void AudioFilterStateVariable2::update(void)
 {
diff --git a/software/src/src/drivers/weegfx.cpp b/software/src/src/drivers/weegfx.cpp
index 956a1189..9a002635 100644
--- a/software/src/src/drivers/weegfx.cpp
+++ b/software/src/src/drivers/weegfx.cpp
@@ -529,10 +529,10 @@ void Graphics::print(uint16_t value, unsigned width)
   print(str);
 }
 
-void Graphics::print(uint32_t value, size_t width)
+void Graphics::print(uint32_t value, unsigned width)
 {
   char *str = itos<uint32_t, false>(value, print_buf, sizeof(print_buf));
-  while (str > print_buf && (size_t)(str - print_buf) >= sizeof(print_buf) - width) *--str = ' ';
+  while (str > print_buf && (unsigned)(str - print_buf) >= sizeof(print_buf) - width) *--str = ' ';
   print(str);
 }
 
diff --git a/software/src/src/extern/dspinst.h b/software/src/src/extern/dspinst.h
index b9ae9880..3b76979c 100644
--- a/software/src/src/extern/dspinst.h
+++ b/software/src/src/extern/dspinst.h
@@ -27,6 +27,8 @@
 #ifndef dspinst_h_
 #define dspinst_h_
 
+#if defined(__ARM_ARCH_7EM__) || defined(KINETISL)
+
 #include <stdint.h>
 
 // computes limit((val >> rshift), 2**bits)
@@ -37,7 +39,7 @@ static inline int32_t signed_saturate_rshift(int32_t val, int bits, int rshift)
 	int32_t out;
 	asm volatile("ssat %0, %1, %2, asr %3" : "=r" (out) : "I" (bits), "r" (val), "I" (rshift));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	int32_t out, max;
 	out = val >> rshift;
 	max = 1 << (bits - 1);
@@ -75,7 +77,7 @@ static inline int32_t signed_multiply_32x16b(int32_t a, uint32_t b)
 	int32_t out;
 	asm volatile("smulwb %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return ((int64_t)a * (int16_t)(b & 0xFFFF)) >> 16;
 #endif
 }
@@ -88,7 +90,7 @@ static inline int32_t signed_multiply_32x16t(int32_t a, uint32_t b)
 	int32_t out;
 	asm volatile("smulwt %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return ((int64_t)a * (int16_t)(b >> 16)) >> 16;
 #endif
 }
@@ -101,7 +103,7 @@ static inline int32_t multiply_32x32_rshift32(int32_t a, int32_t b)
 	int32_t out;
 	asm volatile("smmul %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return ((int64_t)a * (int64_t)b) >> 32;
 #endif
 }
@@ -114,7 +116,7 @@ static inline uint32_t multiply_u32xu32_rshift32(uint32_t a, uint32_t b)
   uint32_t out, tmp;
   asm volatile("umull %0, %1, %2, %3" : "=r" (tmp), "=r" (out) : "r" (a), "r" (b));
   return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
   return 0; // TODO....
 #endif
 }
@@ -127,7 +129,7 @@ static inline int32_t multiply_32x32_rshift32_rounded(int32_t a, int32_t b)
 	int32_t out;
 	asm volatile("smmulr %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return (((int64_t)a * (int64_t)b) + 0x8000000) >> 32;
 #endif
 }
@@ -140,7 +142,7 @@ static inline int32_t multiply_accumulate_32x32_rshift32_rounded(int32_t sum, in
 	int32_t out;
 	asm volatile("smmlar %0, %2, %3, %1" : "=r" (out) : "r" (sum), "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return sum + ((((int64_t)a * (int64_t)b) + 0x8000000) >> 32);
 #endif
 }
@@ -153,7 +155,7 @@ static inline int32_t multiply_subtract_32x32_rshift32_rounded(int32_t sum, int3
 	int32_t out;
 	asm volatile("smmlsr %0, %2, %3, %1" : "=r" (out) : "r" (sum), "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return sum - ((((int64_t)a * (int64_t)b) + 0x8000000) >> 32);
 #endif
 }
@@ -167,7 +169,7 @@ static inline uint32_t pack_16t_16t(int32_t a, int32_t b)
 	int32_t out;
 	asm volatile("pkhtb %0, %1, %2, asr #16" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return (a & 0xFFFF0000) | ((uint32_t)b >> 16);
 #endif
 }
@@ -180,7 +182,7 @@ static inline uint32_t pack_16t_16b(int32_t a, int32_t b)
 	int32_t out;
 	asm volatile("pkhtb %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return (a & 0xFFFF0000) | (b & 0x0000FFFF);
 #endif
 }
@@ -193,7 +195,7 @@ static inline uint32_t pack_16b_16b(int32_t a, int32_t b)
 	int32_t out;
 	asm volatile("pkhbt %0, %1, %2, lsl #16" : "=r" (out) : "r" (b), "r" (a));
 	return out;
-#elif defined(KINETISL)
+#else /* portable fallback (XLOC2 emulator host build) */
 	return (a << 16) | (b & 0x0000FFFF);
 #endif
 }
@@ -367,6 +369,7 @@ static inline int32_t FRACMUL_SHL(int32_t x, int32_t y, int z)
 #endif
 
 //get Q from PSR
+#if defined (__ARM_ARCH_7EM__)
 static inline uint32_t get_q_psr(void) __attribute__((always_inline, unused));
 static inline uint32_t get_q_psr(void)
 {
@@ -374,8 +377,12 @@ static inline uint32_t get_q_psr(void)
   asm ("mrs %0, APSR" : "=r" (out));
   return (out & 0x8000000)>>27;
 }
+#else
+static inline uint32_t get_q_psr(void) { return 0; }
+#endif
 
 //clear Q BIT in PSR
+#if defined (__ARM_ARCH_7EM__)
 static inline void clr_q_psr(void) __attribute__((always_inline, unused));
 static inline void clr_q_psr(void)
 {
@@ -383,6 +390,14 @@ static inline void clr_q_psr(void)
   asm ("mov %[t],#0\n"
        "msr APSR_nzcvq,%0\n" : [t] "=&r" (t)::"cc");
 }
+#else
+static inline void clr_q_psr(void) {}
+#endif
+
 
 
+#else
+// Host build (XLOC2 emulator): unified portable intrinsics
+#include <xemu_dspinst.h>
+#endif
 #endif
diff --git a/software/src/util/util_math.h b/software/src/util/util_math.h
index 474cff63..45ba5322 100644
--- a/software/src/util/util_math.h
+++ b/software/src/util/util_math.h
@@ -46,6 +46,9 @@ using simfloat = int32_t;
  *
  */
 constexpr int Proportion(const int numerator, const int denominator, const int max_value) {
+    // ARM integer division by zero yields 0 (no trap); replicate that on
+    // hosts where it would raise SIGFPE.
+    if (denominator == 0) return 0;
     simfloat proportion = int2simfloat((int32_t)abs(numerator)) / (int32_t)denominator;
     int scaled = simfloat2int(proportion * max_value);
     return numerator >= 0 ? scaled : -scaled;
@@ -72,6 +75,7 @@ constexpr int Atten(int8_t att) {
 #define MOD_8(n, div) \
   FAST_FP_MOD(n, div, 8)
 
+#if defined (__ARM_ARCH_7EM__)
 inline int32_t SSAT16(int32_t value) __attribute__((always_inline));
 inline int32_t SSAT16(int32_t value) {
   int32_t result;
@@ -117,6 +121,40 @@ static inline uint32_t uhadd16(uint32_t a, uint32_t b)
   asm volatile("uhadd16 %0, %1, %2" : "=r" (out) : "r" (a), "r" (b));
   return out;
 }
+#else
+// Portable fallbacks for host builds (XLOC2 emulator)
+inline int32_t SSAT16(int32_t value) {
+  if (value > 32767) return 32767;
+  if (value < -32768) return -32768;
+  return value;
+}
+
+inline uint32_t USAT16(uint32_t value) {
+  return value > 65535u ? 65535u : value;
+}
+
+inline uint32_t USAT16(int32_t value) {
+  if (value < 0) return 0;
+  return value > 65535 ? 65535u : (uint32_t)value;
+}
+
+static inline uint32_t multiply_u32xu32_rshift24(uint32_t a, uint32_t b)
+{
+  return (uint32_t)(((uint64_t)a * (uint64_t)b) >> 24);
+}
+
+static inline uint32_t multiply_u32xu32_rshift(uint32_t a, uint32_t b, uint32_t shift)
+{
+  return (uint32_t)(((uint64_t)a * (uint64_t)b) >> shift);
+}
+
+static inline uint32_t uhadd16(uint32_t a, uint32_t b)
+{
+  uint32_t lo = (((a & 0xFFFF) + (b & 0xFFFF)) >> 1) & 0xFFFF;
+  uint32_t hi = (((a >> 16) + (b >> 16)) >> 1) & 0xFFFF;
+  return (hi << 16) | lo;
+}
+#endif
 
 template <typename T, T smoothing>
 struct SmoothedValue {
